# 🎯 คู่มือการใช้งาน Demo - Advance Agent for Cybersecurity

## 📋 ภาพรวม

Demo นี้แสดงการทำงานของระบบ Cybersecurity Agent ตาม Software Requirements Specification ที่กำหนด

## 🚀 การเริ่มต้นใช้งาน

### 1. ติดตั้ง Dependencies

```bash
cd cybersecurity_demo
pip install -r requirements.txt
```

### 2. รันระบบ

```bash
python app.py
```

### 3. เปิดเบราว์เซอร์

- Dashboard: http://localhost:5000
- Knowledge Base: http://localhost:5000/knowledge
- Logs: http://localhost:5000/logs
- Agents: http://localhost:5000/agents

## 📚 Use Cases ที่ Demo แสดง

### UC-ADM-01: นำเข้าเอกสาร (Knowledge Management)

**ขั้นตอนทดสอบ:**

1. ไปที่หน้า **Knowledge Base** (http://localhost:5000/knowledge)
2. คลิก "เลือกไฟล์เอกสาร"
3. เลือกไฟล์ PDF หรือ DOCX (หรือใช้ไฟล์ตัวอย่างใน `sample_documents/`)
4. คลิก "อัปโหลดเอกสาร"
5. ระบบจะแสดงสถานะการประมวลผล
6. เอกสารจะปรากฏในตารางพร้อมสถานะ "processed"

**สิ่งที่เกิดขึ้นเบื้องหลัง:**
- ระบบรับไฟล์และบันทึกลง `uploads/`
- จำลองการแปลงเป็น Vector Embedding
- จัดเก็บข้อมูลใน Vector Database (จำลอง)
- อัปเดตสถิติ Knowledge Base

### UC-ADM-02: ตั้งค่าการรับข้อมูล Log (Log Management)

**ขั้นตอนทดสอบ:**

1. ไปที่หน้า **Agents** (http://localhost:5000/agents)
2. คลิกที่ Agent ที่ต้องการตั้งค่า (เช่น Firewall-Agent-01)
3. เลือกโปรโตคอล (UDP หรือ TCP)
4. กำหนดพอร์ต (ค่าเริ่มต้น: 514)
5. คลิก "บันทึกการตั้งค่า"
6. Agent จะเปลี่ยนสถานะเป็น "Active"

**สิ่งที่เกิดขึ้นเบื้องหลัง:**
- ระบบบันทึกการตั้งค่า Agent
- Agent พร้อมรับข้อมูล Syslog
- เริ่มรวบรวม Logs แบบ Real-time

### UC-ADM-03: ดู Dashboard แบบ Real-time (System Monitoring)

**ขั้นตอนทดสอบ:**

1. ไปที่หน้า **Dashboard** (http://localhost:5000)
2. สังเกตการแสดงผลสถิติ 4 ส่วน:
   - จำนวนเอกสารใน Knowledge Base
   - จำนวน Logs ที่รวบรวม
   - จำนวน Active Agents
   - System Health (%)
3. ดูตาราง Real-time Logs ที่อัปเดตอัตโนมัติ
4. ตรวจสอบสถานะ Agents ทางด้านขวา

**สิ่งที่เกิดขึ้นเบื้องหลัง:**
- ระบบจำลองการรับ Syslog ทุก 5 วินาที
- WebSocket ส่งข้อมูลแบบ Real-time ไปยัง Dashboard
- Dashboard อัปเดตอัตโนมัติโดยไม่ต้อง refresh หน้า

## 🎨 คุณสมบัติเด่น

### 1. Real-time Updates
- ใช้ WebSocket (Socket.IO) สำหรับการอัปเดตแบบ Real-time
- Logs ใหม่จะปรากฏทันทีโดยไม่ต้อง refresh

### 2. Responsive Design
- ใช้ Bootstrap 5 สำหรับ UI ที่สวยงามและ responsive
- รองรับการใช้งานบนมือถือและแท็บเล็ต

### 3. Data Visualization
- แสดงสถิติแบบ Real-time
- กราฟและตารางที่อ่านง่าย

### 4. Log Filtering
- กรองตามระดับความรุนแรง (INFO, WARNING, CRITICAL)
- กรองตามแหล่งที่มา (Firewall, Server, Router)
- ค้นหาด้วย keyword

## 🧪 การทดสอบ

### Scenario 1: อัปโหลดเอกสารหลายไฟล์
1. อัปโหลดไฟล์ PDF
2. อัปโหลดไฟล์ DOCX
3. ตรวจสอบว่าทั้งสองไฟล์ปรากฏในตาราง
4. ดูสถิติที่อัปเดต

### Scenario 2: ตั้งค่า Multiple Agents
1. ตั้งค่า Firewall-Agent-01 ให้ใช้ UDP:514
2. ตั้งค่า Windows-Server-01 ให้ใช้ TCP:514
3. ตรวจสอบสถานะ Active ของทั้งสอง Agents

### Scenario 3: Monitor Real-time Logs
1. เปิดหน้า Dashboard
2. สังเกต Logs ที่เพิ่มขึ้นทุก 5 วินาที
3. ดูสถิติที่เปลี่ยนแปลง
4. ไปที่หน้า Logs เพื่อดูรายละเอียดเพิ่มเติม

## 📊 ข้อมูลที่จำลอง

### Agents (3 ตัว)
- Firewall-Agent-01 (192.168.1.10) - Active
- Windows-Server-01 (192.168.1.20) - Active
- Router-Agent-01 (192.168.1.1) - Inactive

### Log Templates
- Firewall blocked connection
- Successful login
- Failed authentication attempt
- Port scan detected
- Malware signature detected

### Severity Levels
- INFO: ข้อมูลทั่วไป
- WARNING: คำเตือน
- CRITICAL: ภัยคุกคามร้ายแรง

## 🔧 การปรับแต่ง

### เปลี่ยนความถี่ในการรับ Logs
แก้ไขใน `app.py`:
```python
time.sleep(5)  # เปลี่ยนเป็นจำนวนวินาทีที่ต้องการ
```

### เพิ่ม Agent ใหม่
แก้ไขใน `app.py`:
```python
system_data['agents'].append({
    'id': 4,
    'name': 'New-Agent',
    'status': 'inactive',
    'ip': '192.168.1.30'
})
```

## 🎓 สมาชิกโครงการ

1. นายภูวิศ จารุรัตน์กิจ (67056056)
2. นายสิรภพ กิจเจริญรุ่งโรจน์ (67056078)
3. นายสุทธิ ดิลกเลิศพลากร (67056082)

## 📝 หมายเหตุ

- Demo นี้เป็นการจำลองการทำงานของระบบจริง
- ข้อมูล Logs และ Vector Embeddings เป็นข้อมูลจำลอง
- สำหรับการใช้งานจริง ต้องเชื่อมต่อกับ Vector Database และ Syslog Server จริง

## 🐛 การแก้ไขปัญหา

### ปัญหา: Port 5000 ถูกใช้งานแล้ว
```bash
# เปลี่ยน port ใน app.py
socketio.run(app, debug=True, host='0.0.0.0', port=5001)
```

### ปัญหา: ติดตั้ง dependencies ไม่สำเร็จ
```bash
# ใช้ virtual environment
python -m venv venv
source venv/bin/activate  # macOS/Linux
# หรือ
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

## 🎉 สนุกกับการทดสอบ!
