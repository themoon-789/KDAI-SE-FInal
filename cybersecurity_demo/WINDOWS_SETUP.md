# 🪟 คำแนะนำติดตั้งบน Windows 11

## 📋 ข้อกำหนดเบื้องต้น

### 1. ติดตั้ง Python 3.8+
ดาวน์โหลดจาก: https://www.python.org/downloads/

**สำคัญ:** ✅ เลือก "Add Python to PATH" ตอนติดตั้ง

### 2. ตรวจสอบ Python
เปิด Command Prompt (cmd) หรือ PowerShell:
```cmd
python --version
```
ควรแสดง: `Python 3.x.x`

## 🚀 การติดตั้งและรัน

### วิธีที่ 1: ใช้ Command Prompt (แนะนำ)

1. **แตกไฟล์ zip**
   - คลิกขวาที่ไฟล์ → Extract All
   - เลือกโฟลเดอร์ที่ต้องการ

2. **เปิด Command Prompt**
   - กด `Win + R`
   - พิมพ์ `cmd` แล้วกด Enter

3. **ไปที่โฟลเดอร์โปรเจค**
   ```cmd
   cd C:\path\to\cybersecurity_demo
   ```

4. **ติดตั้ง Dependencies**
   ```cmd
   pip install -r requirements.txt
   ```

5. **รันแอป**
   ```cmd
   python app.py
   ```

6. **เปิดเบราว์เซอร์**
   ```
   http://localhost:5001
   ```

### วิธีที่ 2: ใช้ PowerShell

1. **เปิด PowerShell**
   - คลิกขวาที่โฟลเดอร์ → "Open in Terminal"

2. **ติดตั้งและรัน**
   ```powershell
   pip install -r requirements.txt
   python app.py
   ```

### วิธีที่ 3: ใช้ไฟล์ .bat (ง่ายที่สุด)

**ดับเบิลคลิกที่:** `START_DEMO.bat`

## 🔧 แก้ไขปัญหา

### ปัญหา: Python ไม่พบ
```cmd
# ลองใช้ python3 แทน
python3 --version
python3 app.py
```

### ปัญหา: pip ไม่พบ
```cmd
# ติดตั้ง pip
python -m ensurepip --upgrade
```

### ปัญหา: Port 5001 ถูกใช้งาน
แก้ไขใน `app.py` บรรทัดสุดท้าย:
```python
socketio.run(app, debug=True, host='0.0.0.0', port=5002)
```

### ปัญหา: Firewall บล็อก
- เปิด Windows Defender Firewall
- Allow Python ผ่าน Firewall

### ปัญหา: ModuleNotFoundError
```cmd
# ติดตั้งใหม่
pip install --upgrade -r requirements.txt
```

## 📁 โครงสร้างไฟล์

```
cybersecurity_demo/
├── app.py                    # ไฟล์หลัก - รันไฟล์นี้
├── requirements.txt          # Dependencies
├── START_DEMO.bat           # ดับเบิลคลิกเพื่อรัน (Windows)
├── WINDOWS_SETUP.md         # คู่มือนี้
├── templates/               # HTML templates
├── uploads/                 # เก็บไฟล์ที่อัปโหลด (สร้างอัตโนมัติ)
└── data/                    # เก็บข้อมูล (สร้างอัตโนมัติ)
```

## 🎯 ทดสอบว่าทำงานได้

1. เปิดเบราว์เซอร์ไปที่: http://localhost:5001
2. ควรเห็นหน้า Dashboard
3. ลองคลิกเมนูต่างๆ:
   - Knowledge Base
   - Logs
   - Agents

## 🌐 URL ทั้งหมด

```
Dashboard:       http://localhost:5001
Knowledge Base:  http://localhost:5001/knowledge
Logs:            http://localhost:5001/logs
Agents:          http://localhost:5001/agents
```

## 🛑 หยุดการทำงาน

กด `Ctrl + C` ใน Command Prompt/PowerShell

## 💡 Tips สำหรับ Windows

### ใช้ Virtual Environment (แนะนำ)
```cmd
# สร้าง virtual environment
python -m venv venv

# เปิดใช้งาน
venv\Scripts\activate

# ติดตั้ง dependencies
pip install -r requirements.txt

# รันแอป
python app.py

# ปิดใช้งาน
deactivate
```

### เปิดหลายหน้าต่าง Terminal
- กด `Win + R` → พิมพ์ `cmd` → Enter
- ทำซ้ำเพื่อเปิดหน้าต่างใหม่

### ใช้ Windows Terminal (แนะนำ)
- ดาวน์โหลดจาก Microsoft Store
- รองรับ tabs และสวยงามกว่า

## 📊 ตรวจสอบว่าระบบทำงาน

### ทดสอบ API
เปิด PowerShell ใหม่:
```powershell
# ทดสอบ API
Invoke-WebRequest -Uri http://localhost:5001/api/stats | Select-Object -Expand Content
```

หรือใช้เบราว์เซอร์:
```
http://localhost:5001/api/stats
http://localhost:5001/api/agents
```

## 🎓 Use Cases ที่ทดสอบได้

### UC-ADM-01: อัปโหลดเอกสาร
1. ไปที่ http://localhost:5001/knowledge
2. เลือกไฟล์ (PDF, DOCX, TXT)
3. คลิก "อัปโหลดเอกสาร"
4. ✅ เอกสารจะปรากฏในตาราง

### UC-ADM-02: ตั้งค่า Agent
1. ไปที่ http://localhost:5001/agents
2. คลิกที่ Agent
3. เลือก Protocol และ Port
4. คลิก "บันทึกการตั้งค่า"
5. ✅ Agent เปลี่ยนเป็น Active

### UC-ADM-03: ดู Dashboard
1. ไปที่ http://localhost:5001
2. ✅ ดูสถิติแบบ Real-time
3. ✅ Logs ใหม่ทุก 5 วินาที

## 🔐 Security Note

- ระบบนี้เป็น Demo สำหรับการพัฒนา
- ไม่ควรใช้ใน Production โดยตรง
- Debug mode เปิดอยู่ (ปิดก่อนใช้งานจริง)

## 📞 ต้องการความช่วยเหลือ?

อ่านเอกสารเพิ่มเติม:
- `README.md` - ข้อมูลโครงการ
- `DEMO_GUIDE.md` - คู่มือใช้งานโดยละเอียด
- `QUICK_START.md` - เริ่มต้นด่วน

## ✅ Checklist

- [ ] ติดตั้ง Python 3.8+ แล้ว
- [ ] เพิ่ม Python ใน PATH แล้ว
- [ ] แตกไฟล์ zip แล้ว
- [ ] ติดตั้ง dependencies แล้ว (`pip install -r requirements.txt`)
- [ ] รันแอปได้ (`python app.py`)
- [ ] เปิดเบราว์เซอร์ได้ (http://localhost:5001)
- [ ] ทดสอบทุก Use Cases แล้ว

---

**สนุกกับการทดสอบบน Windows 11! 🎉**
